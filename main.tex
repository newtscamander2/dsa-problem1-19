\documentclass{article}

\usepackage[a4paper]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[noend]{algpseudocode}
\usepackage{amsthm}
\usepackage{colortbl}
\usepackage{graphicx}
\usepackage{lineno}
\usepackage[textwidth=4cm]{todonotes}
\usepackage{float}

\geometry{left=3cm, right=6cm, top=3cm, bottom=3cm, marginparwidth=4cm} 

\linenumbers

\setuptodonotes{noline, color=green!15, size=\scriptsize}

\newtheorem*{exercise}{Exercise}
\newcommand{\grey}{\cellcolor{black!25}}

\title{Algorithms and Data Structures \\
  Handin 0: Pair sum}

\author{
    202605673 Lukas Alstrup \\
    202605116 Thomas Seeberg Hansen \\
    202606296 Frederik Dommer Løvbjerg Plum
}

%\date{September 1, 2025}  % Use \date to set a specific date

\begin{document}

\maketitle
\begin{exercise}[Squared]
	Prove that the algorithm Squared(n) computes $s = n^2$. Find an appropriate loop invariant I, and use this to conclude that $s = n^2$ when the algorithm terminates.
\end{exercise}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.3\linewidth]{algo.png}
\end{figure}

\section*{Comment}
To complete this assignment we have to prove that

\section*{Our solution}
To start with we want to prove that $s = n^2$.

From the pseudo code we can see that $i$ a incrementing sum which we can write as $\sum_{0}^{n}1$. $s$ is also a incrementing sum of $i$ which can be written as $s = \sum_{f=0}^{n}f$.

$s$ is at the end of our program set to $s = s + s - n$, which we can write as
$$s_n = 2\sum_{f=0}^{n}f - n$$
From the mathematics cheats sheet in the supplementary notes we know that $\sum_{i=1}^{n}i = \frac{n(n+1)}{2}$.

We can therefore write
$$s_n = 2\frac{n(n+1)}{2}-n$$
Which we can easily simplify even further
$$s_n = n(n+1) -n$$
$$s_n = n^2 + n - n$$
$$s_n = n^2$$

Which is proof that $s = n^2$.

One easy loop invariant we can see is that $i$ is initialized as $0$. Therefore we can set the loop invariant $0 \leq i$. From the proof of $s = n^2$ we know that $i = \sum_{0}^{n}1=n$. We can therefore also say that $i \leq n$.

The loop invariant for the given loop is therefore

\begin{equation}
	I(S_i): 0 \leq i \leq n \land 0 \leq s \leq \sum_{f=0}^{n}f
\end{equation}

\vspace{5mm}
\subsubsection*{Proof by induction}
Base case $n=1$:
$i=0, s = 0, n \geq 0$
Let $S_{i}$ represent the state of the while-loop at a given iteration $i$ thus $S_0$ will be the first iteration of the while-loop after the proceeding code has executed.
To prove the loop invariant $I$ using proof by induction\dots

For the base case (which is the start of our loop) we can verify $0 \leq i \leq n$ which becomes $0 \leq 0 \leq 0$ which is true. We can also verify $0 \leq s \leq \sum_{f=0}^{n}f$ which becomes $0 \leq 0 \leq 0$ since if $n=0$ (which is a possibility since $n\geq0$ is the only requirement for $n$) then $\sum_{f=0}^{n}f = 0$.
Our loop invariant therefore holds at the start of our loop.

To prove by induction we assume that $I(S) \Rightarrow I(S')$, where $S'$ is the next step in our loop and $i' = i + 1, s' = s + i$.

$$0 \leq i \leq n \land 0 \leq s \leq \sum_{i=0}^{n}i \Rightarrow 0 \leq i+1 \leq n \land 0 \leq s+i \leq \sum_{f=0}^{n}f$$
The loop stops when $i < n$ is no longer true. $i$ is incremented in the loop. This means that the last iteration of the loop $i=n, s=\sum_{f=0}^{n}$.
$$$$

\end{document}
